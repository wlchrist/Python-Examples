first_name = input("Type your first name")
print(first_name)
last_name = input("Type your last name")
print(last_name)

phrase = "Hello World"

sentence = "Help me"

print(sentence.upper())
print(sentence.lower())
print(sentence.capitalize())
print(sentence.count("e"), sentence.count("h"))

# String Methods are used to manipulate strings
# Strings are a fundamental data type
# This means that they cannot be broken into smaller data types
# Characters in a string are numbered by the position they're in
# You can use a backslash as a delimiter